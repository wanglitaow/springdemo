<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
    <!--定义日志文件的存储地址 勿在 LogBack 的配置中使用相对路径-->
    <property name="LOG_HOME" value="logs/"/>
    <springProperty scope="context" name="springAppName" source="spring.application.name"/>
    <!-- 控制台输出 -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [${springAppName:-}] [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>${LOG_HOME}/spring_demo.log.%d{yyyy-MM-dd}</FileNamePattern>
            <MaxHistory>10</MaxHistory>
        </rollingPolicy>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </layout>
        <!--日志文件最大的大小-->
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>100MB</MaxFileSize>
        </triggeringPolicy>
    </appender>


    <logger name="main.java.springboot.controller" additivity="false">
        <level value="debug"/>
        <appender-ref ref="file"/>
        <!--<appender-ref ref="logstashUdp"/>-->
        <appender-ref ref="console"/>
    </logger>

    <logger name="main.java.springboot.serivce" additivity="false">
        <level value="debug"/>
        <appender-ref ref="file"/>
        <!--<appender-ref ref="logstashUdp"/>-->
        <appender-ref ref="console"/>
    </logger>

    <logger name="com.bilibili.mall.search.dao" additivity="false">
        <level value="debug"/>
        <appender-ref ref="file"/>
        <!--<appender-ref ref="logstashUdp"/>-->
        <appender-ref ref="console"/>
    </logger>

    <!-- 这就是上边logger没有管到的情况下 root默认接管所有logger -->
    <root>
        <level value="info"/>
        <appender-ref ref="file"/>
        <!--<appender-ref ref="logstashUdp"/>-->
        <appender-ref ref="console"/>
    </root>
</configuration>
